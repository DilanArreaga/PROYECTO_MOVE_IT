/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí podemos poner configuraciones extra más adelante
  reactStrictMode: true, // Ayuda a detectar errores en el código
};

module.exports = nextConfig;